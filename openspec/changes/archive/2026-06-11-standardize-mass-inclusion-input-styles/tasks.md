## 1. Modify Shared UI Components

- [x] 1.1 Update styling and label layout of `src/app/shared/components/input/input.ts` to use spacious `py-2.5 px-3`, `rounded-lg`, and `text-sm` styles, adjusting padding for icons.
- [x] 1.2 Update styling and label layout of `src/app/shared/components/select/select.ts` to use spacious `py-2.5 px-3`, `rounded-lg`, and `text-sm` styles.
- [x] 1.3 Update styling and label layout of `src/app/shared/components/textarea/textarea.ts` to use spacious `py-2.5 px-3`, `rounded-lg`, and `text-sm` styles.

## 2. Refactor Dashboard Filters Panel

- [x] 2.1 Update imports in `src/app/ui/views/dashboard/components/dashboard-filters-panel.ts` to include `Input` and `Select`.
- [x] 2.2 Replace raw HTML inputs and selects in `src/app/ui/views/dashboard/components/dashboard-filters-panel.ts` with `<app-input>` and `<app-select>`.

## 3. Verification

- [x] 3.1 Run tests or build the application to confirm there are no compilation or styling regressions.
